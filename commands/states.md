---
description: Построить матрицу состояний для файла, компонента или эндпоинта и открыть отчёт
---

Построй матрицу состояний для: $ARGUMENTS

Используй скилл `state-matrix`. Порядок: граница → параметры → окружение →
значения по каталогу → ограничения → переходы → `validate.py` → `engine.py` → `serve.py`.

Модель положи в `.states/models/`, отчёт подними на localhost и дай ссылку.
Числа называй только те, что напечатал движок.
